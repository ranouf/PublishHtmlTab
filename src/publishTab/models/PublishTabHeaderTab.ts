/**
 * Represents a single tab rendered in the PublishTab header.
 */
export interface PublishTabHeaderTab {
  downloadArchiveFileName?: string;
  id: string;
  label: string;
  showDownloadBadge?: boolean;
}
